# Frontend

This folder is reserved for the UI layer that will consume the backend API.

The backend is ready to accept calls while the final model/provider decision is still being finalized.

## Next step

- Connect the frontend to /health and /reconcile-record
- render reconciliation results in a simple dashboard
- keep provider selection isolated from the UI
